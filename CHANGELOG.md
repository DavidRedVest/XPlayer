# 更新日志

## v0.1.10（2026-08-25）

### 新增

- 录像封装格式切换：TS / MKV / MP4，右键菜单新增"录制格式"子菜单，默认 MP4，用 `QSettings` 持久化（只影响之后新开始的录像，不打断正在写的文件）。
- 全局音量控制：顶部滑块 + 百分比数字，直播/回放页面通用，持久化保存。
- 截图：右键菜单"截图"，直播、回放格子都能用，按视频源原始分辨率存成 PNG，保存到独立的 `screenshots` 目录，弹窗可以直接"打开文件所在位置"。
- 录像/截图文件名改为自描述格式：`{日期}_{时间}_[{摄像头名}_]win{窗口编号}[_part{分段序号}]`，取代原来纯数字后缀（分不清哪个是序号、哪个是窗口号）。
- 新增命令行工具 `tools/xrecorder_probe`，绕开 GUI 直接验证 `XRecorder` 录像功能。

### 修复

- 修复右键菜单在"点击别处取消、不选择任何菜单项"时可能被误判为选中了某个菜单项、进而误触发对应操作的 bug。
- 修复截图在同一秒内被再次触发时会静默覆盖前一张的问题。

### 开发中遇到的问题与解决方案

1. **为什么要支持 TS/MKV**：现有的 MP4 录像在异常断电/进程被杀时会整段报废——MP4 的索引（`moov`）只在收尾时一次性写完，异常中断这段就没有索引，基本无法播放。用 `kill -9` 实测对比三种格式：MP4 文件事后 `ffprobe` 直接报 "moov atom not found / Invalid data found"（完全读不出来），TS/MKV 都能正常读出已经写下去的部分（`format_name`/视频音频轨道信息完整），符合预期。

2. **文件名方案的取舍**：最初提案是"摄像头名字放最前面"（按摄像头分组浏览更方便），最终改成"时间戳在前，窗口编号仍然保留在后面兜底"——保持跨摄像头按时间排序的既有习惯，同时窗口编号能在摄像头改名/多个摄像头重名时兜底区分。摄像头名字统一走 `QString::toUtf8()`（而不是 `toLocal8Bit()`）转换成文件名字节，跟这个项目里 RTSP 地址等其它路径字符串的转换方式保持一致，避免中文在非中文系统区域设置下写乱码；同时按 UTF-16 code unit（而不是字节）截断到 8 个字符，避免把一个多字节字符从中间切断，并过滤 Windows 文件名非法字符（`/\:*?"<>|`）。

3. **截图分辨率的实现选择**：没有用最简单的 `QOpenGLWidget::grabFramebuffer()`——那样截出来的分辨率会受限于格子在屏幕上的实际显示大小，宽高比不一致时黑边也会被截进去。改成直接从已经按视频源分辨率建好的 GPU 纹理（`tex_y_/tex_u_/tex_v_`）用 `glGetTexImage()` 读回 YUV 数据，经 swscale 转 RGB 后交给 `QImage::save()` 存 PNG——不需要在解码回调那条热路径上为了"万一要截图"常驻多存一份 CPU 拷贝，零额外的每帧开销，而且截图分辨率天然就是视频源分辨率。

4. **右键菜单 nullptr 误触发**：`QMenu::exec()` 在用户点击菜单外、没有选中任何选项时返回 `nullptr`；但如果某个 `QAction*`（比如回放网格里没有的"录制当前画面"）因为当前网格/格子状态压根没被创建，它本身也是 `nullptr`——`chosen == that_action` 这种写法在两边都是 `nullptr` 时会恰好成立，被误判成"选中了它"，进而触发那个操作本不该触发的副作用。复现场景：回放页面右键弹出菜单，点别处取消，却弹出了"请先单击选中一个正在直播的画面"的提示。这个问题在原来的代码里已经存在（这次改动只是又新增了三个同样会受影响的菜单项），修复方式是在分发判断之前先 `if (!chosen) return;`，点别处取消时整段跳过后面所有判断。

## v0.1.1 – v0.1.9（Windows 首次真机联调）

在 Windows 真机（NVIDIA GTX 1050 Ti）上暴露出三个此前只在 macOS 上跑没发现的问题：

- **RTSP 连不上**：漏调 `avformat_network_init()`，网络协议没有注册。
- **打开 RTSP 流或本地文件播放时崩溃**（`0xC0000005` 访问违例）：根因是渲染代码用 `glVertexAttribPointer` 直接指向 CPU 端的静态数组、没有绑定真正的顶点缓冲区（VBO）——这在绑了 VAO 的情况下，NVIDIA 的 Windows 驱动会把这个 CPU 地址当成显存偏移量去解释，`glDrawArrays` 取顶点数据时越界访问，真实崩溃；macOS 的 Metal 后端"碰巧"容忍这种写法，掩盖了问题一直没发现。改成真正的 `QOpenGLBuffer` 顶点缓冲解决。
- **没有声音/声音异常**：Windows 音频设备实际同意的格式是 `AUDIO_F32`，不是请求的 `AUDIO_S16`，但重采样目标格式写死成 S16，格式不匹配导致播放出来接近静音。第一次修复尝试（改用 `SDL_OpenAudioDevice` 强制锁定格式）反而把 macOS 和 Windows 的声音都搞坏了（表面日志干净、实际没有声音输出，这个教训是"日志正常"不能代替"实测有真实的非零采样"）；最终方案是保留 `SDL_OpenAudio()`（macOS 上已验证一直工作良好），转而让重采样目标格式跟随设备实际同意的格式，并新增一个针对 F32 的混音函数（`SDL_MixAudio()` 本身只支持 S16）。

## v0.1.0

初始版本：CMake + Qt6 跨平台重写，多摄像头直播网格、录像、回放（seek/单帧步进/倍速+音调保持），GitHub Actions 三平台（Windows/Linux/macOS）自动构建发布流水线。
